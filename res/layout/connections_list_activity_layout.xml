<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent"
    android:orientation="vertical"
    android:padding="7dp"
    android:weightSum="100" >

    <RelativeLayout
        android:id="@+id/addNewConnectionBtn"
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight="20"
        android:background="@drawable/orange_button_selector" >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:drawableLeft="@drawable/add"
            android:drawablePadding="12dp"
            android:gravity="center"
            android:text="@string/add_new_connection_lbl"
            android:textColor="@color/white"
            android:textSize="@dimen/add_new_aconnection_btn" />
    </RelativeLayout>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight="2" />

    <ListView
        android:id="@+id/connectionsListView"
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight="60"
        android:background="@color/transparent"
        android:divider="@color/transparent"
        android:dividerHeight="1dp" >
    </ListView>

</LinearLayout>