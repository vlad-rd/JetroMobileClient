<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:orientation="vertical" >

    <RelativeLayout
        android:id="@+id/add_new_connection_button"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_margin="@dimen/list_padding"
        android:background="@drawable/orange_button_selector" >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:drawableLeft="@drawable/add"
            android:drawablePadding="12dp"
            android:gravity="center"
            android:text="@string/add_new_connection_lbl"
            android:textColor="@android:color/white"
            android:textSize="@dimen/button_add_new_aconnection_text_size" />
    </RelativeLayout>

    <ListView
        android:id="@+id/connections_list"
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight="1"
        android:divider="@android:color/transparent"
        android:dividerHeight="@dimen/list_item_padding"
        android:padding="@dimen/list_padding"
        android:scrollbarStyle="outsideOverlay" />

</LinearLayout>